{
  "name": "licsystem",
  "version": "1.0.0",
  "private": true,
  "description": "LICSYSTEM - licitacoes (Vite + Vercel + Firebase + Gemini)",
  "scripts": {
    "dev": "vite",
    "dev:vite": "vite",
    "dev:api": "npx --yes vercel dev --listen 3000",
    "dev:all": "concurrently -k -n vite,api -c cyan,magenta \"vite\" \"npx --yes vercel dev --listen 3000\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "html-minifier-terser": "^7.2.0",
    "javascript-obfuscator": "^5.5.0",
    "terser": "^5.49.0",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
